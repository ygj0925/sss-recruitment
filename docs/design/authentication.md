# 蓝色认证体验设计规范

> 适用范围：`src/pages/auth/` 下的 uni-app 认证页面。视觉结构参考产品提供的移动端认证原型；原型中的绿色品牌交互已统一替换为蓝色，微信入口保留官方绿色。

## 1. 目标与边界

认证体验覆盖以下页面与前端状态：

- 账号密码登录：手机号、密码显隐、协议确认、忘记密码、验证码登录入口。
- 验证码登录/注册：手机号校验、图形验证码弹层、60 秒演示倒计时与协议确认。
- 微信小程序登录：调用 `uni.login` 获取临时 code；开发期安全分流至绑定手机号页。
- 绑定手机号、重置密码、设置密码和用户协议。

当前版本没有接入真实短信、注册、绑定手机号或密码重置服务。所有演示性成功反馈必须明确说明“服务待接入”，且不得创建账号、修改密码、保存模拟 Token 或修改用户身份。

## 2. 设计令牌

| 用途 | 值 | 使用位置 |
| --- | --- | --- |
| 主色 | `#0957DE` | 主按钮、已选协议、链接强调 |
| 主色按下 | `#0748BE` | 主按钮 active 状态 |
| 主色浅底 | `#E8F1FF` | 选中/弱强调背景 |
| 主按钮禁用 | `#B8D4FF` | 不可提交状态 |
| 页面背景 | `#F7F9FC` | 认证页面画布 |
| 主文字 | `#1F2937` | 标题、输入内容 |
| 次级文字 | `#98A2B3` | 提示、说明、图标 |
| 链接 | `#3B82F6` | 协议、找回密码、切换入口 |
| 错误 | `#E5484D` | 表单错误及失败反馈 |
| 成功 | `#0F9F6E` | 非微信的成功反馈 |
| 微信入口 | `#07C160` | 微信图标，仅作为第三方品牌色 |

项目级主题已在 `src/uni.scss` 和 `src/style/index.scss` 对齐为 `#0957DE`；页面局部常量集中在 `src/pages/auth/auth.ts` 的 `AUTH_THEME`。

## 3. 排版、间距与控件

- 页面默认使用 `rpx`，移动端以单栏为基线。
- 主标题：`44rpx`、700 字重；说明文字：`24rpx`。
- 输入与主按钮高度：`88rpx`；输入框、按钮圆角：`44rpx`（胶囊样式）。
- 弹层圆角：`24rpx`；桌面端认证卡片圆角：`36rpx`。
- 输入框必须保留可见错误文案和红色边框，不能仅通过颜色表达错误。
- 密码显隐、协议勾选、第三方入口均提供不小于 `88rpx` 的触达容器。

## 4. 响应式布局

| 视口 | 布局规则 |
| --- | --- |
| 小于 `768px` | 单栏认证页、居中标题与表单，匹配移动端原型的信息层级。 |
| 大于等于 `768px`（H5） | 左侧蓝色品牌展示区，右侧可滚动认证区域与白色表单卡片。 |
| 小程序 / App | 保持单栏；不依赖浏览器专属的桌面装饰能力。 |

安全区复用现有 UnoCSS `pt-safe`、`pb-safe`。认证页面使用自定义导航栏，以便提供原型中的返回与帮助区域。

## 5. 组件规范

| 组件 | 文件 | 职责 |
| --- | --- | --- |
| 认证外壳 | `components/AuthLayout.vue` | 页面画布、移动/桌面布局、品牌区、标题和返回区。 |
| 输入框 | `components/AuthInput.vue` | 统一输入、错误状态、密码显隐、右侧验证码动作。 |
| 协议勾选 | `components/AuthAgreement.vue` | 可访问的协议确认和协议页入口。 |
| 主按钮 | `components/AuthButton.vue` | 禁用、处理中的主操作。 |
| 第三方入口 | `components/AuthSocialLogin.vue` | 微信、QQ、Apple 的入口展示；本期仅微信具备流程。 |
| 图形验证码 | `components/AuthCaptchaModal.vue` | 开发期图形验证码弹层，正确示例值为 `0793`。 |

新增页面均使用 `definePage({ excludeLoginPath: true })`，保证切换到默认“必须登录”策略时认证流程仍可访问。

## 6. 交互与表单状态

### 账号密码登录

- 手机号必须是 11 位中国大陆手机号；密码长度为 6-20 位。
- 用户未勾选协议时，账号登录和微信登录均显示明确错误并停止操作。
- 账号密码接口尚未接入，因此有效表单提交只显示“服务待接入”反馈。

### 验证码登录、绑定与重置密码

- 发送验证码前先校验手机号，再展示图形验证码弹层。
- 图形验证码正确后开始 60 秒倒计时；此计时器仅为前端演示，不发送真实短信。
- 绑定手机号与设置密码的提交不修改 Token、账户或密码。

### 微信小程序登录

`src/service/auth/wechat-auth.ts` 是唯一的微信登录适配层：

1. 仅在微信小程序端调用 `uni.login({ provider: 'weixin' })` 获取 code。
2. 默认 `VITE_WECHAT_AUTH_MODE=mock`，得到 code 后返回 `binding_required`，前端导航到绑定手机号页。
3. mock 结果不导入或调用 Token Store，因而不可能制造真实登录态。
4. H5/App 显示“请在微信小程序中使用微信登录”；取消授权、无 code、接口未配置均显示可恢复错误。
5. 将来设置 `VITE_WECHAT_AUTH_MODE=api` 后，页面通过受控 `authenticate` 回调调用服务端；只有服务端返回已验证 Token 时，才允许 `useTokenStore().completeExternalLogin()` 写入状态并消费内部 `redirect`。

未来服务端需要明确约定的最小信息如下，接入前不得猜测字段：

- 请求：微信临时 `code`。
- 已认证响应：项目既有单/双 Token 结构。
- 未绑定响应：可被适配层映射为 `binding_required` 的明确业务状态。
- 错误响应：可展示的错误码或错误消息。

## 7. 路由与安全

认证路由常量集中于 `src/router/config.ts`，包含登录、验证码登录/注册、绑定手机号、重置密码、设置密码和协议页。`src/utils/toLoginPage.ts` 使用同一 `LOGIN_PAGE`，避免 Token 失效时跳到已删除的 `/pages/login/index`。

`resolveAuthRedirect()` 仅接受以单个 `/` 开头的内部 uni-app 路由；缺失、损坏、外部 URL 或 `//` 协议相对 URL 都会回退至 `/pages/index/index`。

## 8. 无障碍与内容要求

- 输入框拥有明确 placeholder；错误文本紧邻对应输入框。
- 密码显示/隐藏和协议确认带有语义化标签。
- 禁用按钮不可触发提交；异步操作应显示“处理中”。
- 不用仅颜色传达成功、错误、选中或禁用状态；必须配合文本或勾选标记。
- 文案必须明确区分“开发演示”与真实服务，尤其是短信、注册、绑定、重置密码和微信登录。

## 9. 验收清单

- [ ] 375px 移动视口呈现单栏认证布局。
- [ ] 768px 与 1440px H5 视口呈现品牌展示区和认证卡片。
- [ ] 未勾选协议时，账号登录和微信登录均不能继续。
- [ ] 微信开发者工具中，微信入口请求 `uni.login` 后进入绑定手机号页（mock 模式）。
- [ ] H5 中微信入口不会创建登录态，并提示仅支持微信小程序。
- [ ] 图形验证码、发送冷却、找回密码、设置密码、协议页均可到达。
- [ ] 后端未接入时，任何演示操作均不写入 Token 或用户资料。
